CREATE TABLE IF NOT EXISTS HIT
(
    ID        INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    APP       VARCHAR(100),
    URI       VARCHAR(255),
    IP        VARCHAR(20),
    TIMESTAMP TIMESTAMP
);